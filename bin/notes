#!/usr/bin/env bash

# Only for Windows (TODO)
if command -v start &> /dev/null
then
    start "https://github.com/VinceCabs/dotfiles/tree/master/notes/$1"
else
    echo "Windows or browser not found"
fi